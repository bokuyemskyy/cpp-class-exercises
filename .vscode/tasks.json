{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "cmake -Bbuild && cmake --build build",
            "group": {
                "kind": "build",
            },
        }
    ]
}